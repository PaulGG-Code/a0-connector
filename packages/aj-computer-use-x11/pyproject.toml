[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "aj-computer-use-x11"
version = "1.5"
description = "X11 computer-use backend for a0"
requires-python = ">=3.10"
authors = [
    { name = "PaulGG-Code" },
]
dependencies = [
    "a0>=1.5",
    "mss>=10.1.0",
    "python-xlib>=0.33",
]

[project.entry-points."aj.computer_use_backends"]
x11 = "aj_computer_use_x11.backend:X11_BACKEND_SPEC"

[tool.hatch.build.targets.wheel]
packages = ["src/aj_computer_use_x11"]
