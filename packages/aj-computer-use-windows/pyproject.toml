[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "aj-computer-use-windows"
version = "1.5"
description = "Windows computer-use backend for a0"
requires-python = ">=3.10"
dependencies = [
    "a0>=1.5",
    "dxcam; platform_system == 'Windows'",
    "pillow; platform_system == 'Windows'",
    "pywinauto; platform_system == 'Windows'",
]

[project.entry-points."aj.computer_use_backends"]
windows = "aj_computer_use_windows.backend:WINDOWS_BACKEND_SPEC"

[tool.hatch.build.targets.wheel]
packages = ["src/aj_computer_use_windows"]
